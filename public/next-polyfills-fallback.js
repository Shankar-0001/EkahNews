// Compatibility fallback for environments that request the legacy Next.js
// polyfills alias. Modern browsers ignore this `nomodule` script path, and
// production builds will rewrite to the real hashed chunk when available.
